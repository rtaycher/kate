- merge KWrite and KWriteView (cullmann - done - now KWriteView is only for internal use in KWrite)
- implement all KTextEditor methods
- extend the KTextEditor interfaces to let it have stuff like VConfig?
- obey the mark boolean arguments in all the insert* methods
   -- or --
  discuss if we should get rid of them :-)
